# ─── LOCAL dependencies (install these on your laptop) ───────────────────────
modal                          # Modal SDK — run with: pip install modal

# Actian VectorAI Python client — install from the cloned repo's wheel file:
# pip install ./actiancortex-0.1.0b1-py3-none-any.whl
# (The wheel is in the actian-vectorAI-db-beta repo you clone from GitHub)

# FastAPI — for the API layer (Phase 3)
fastapi[all]
pydantic>=2.0
google-genai>=1.5.0,<2.0.0     # Gemini SDK used by services/gemini_synthesis.py

# PDF report generation
fpdf2>=2.7.0

# SQLite is built into Python — no install needed.

# ─────────────────────────────────────────────────────────────────────────────
# Everything below installs INSIDE the Modal container — do NOT pip install
# these locally. They are defined in vigilant_image in workers/inference.py
# ─────────────────────────────────────────────────────────────────────────────
# ultralytics>=8.3.0           YOLOv11 + ByteTrack      (Modal image)
# transformers>=4.40.0         Florence-2 + CLIP         (Modal image)
# torch>=2.2.0                                           (Modal image)
# torchvision>=0.17.0                                    (Modal image)
# Pillow>=10.0.0                                         (Modal image)
# opencv-python-headless>=4.9.0                          (Modal image)
# numpy                                                  (Modal image)
# einops                       Florence-2 dep            (Modal image)
# timm                         Florence-2 dep            (Modal image)
# huggingface_hub                                        (Modal image)
