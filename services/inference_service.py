#run_detection(frames) -> objects

#run_tracking(detections) -> tracks

#infer_activities(tracks) -> activity_scores

#generate_embeddings(frames) -> vectors

#llm_summarize(window_data) -> caption/json